-- ============================================================================
-- rollback 005 — снимает таблицу auth_sessions.
-- ★ После этого все авторизованные пользователи будут разлогинены при
--   следующем запросе (verifyJwt не найдёт сессию).
-- ============================================================================

DROP TABLE IF EXISTS private_data.auth_sessions;
