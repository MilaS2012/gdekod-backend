-- ============================================================================
-- rollback 007 — снимает колонку user_agent_summary.
-- На проде безопасно при условии, что данные в колонке можно потерять
-- (UI-метка регенерируется при следующих INSERT через parseUserAgent).
-- ============================================================================

ALTER TABLE private_data.auth_sessions
    DROP COLUMN IF EXISTS user_agent_summary;
